import { useEffect, useState } from 'react';
import { Alert, Box, Button, Dialog, DialogActions, DialogContent, DialogTitle, Paper, Table, TableBody, TableCell, TableHead, TableRow, TextField, Typography } from '@mui/material';
import { roleApi } from '../services/crmApi';

const unwrap=d=>Array.isArray(d)?d:d?.data??d?.content??[];
export default function Role(){const[rows,setRows]=useState([]),[open,setOpen]=useState(false),[editing,setEditing]=useState(null),[form,setForm]=useState({name:'',code:'',description:''}),[error,setError]=useState('');const load=async()=>{try{const r=await roleApi.list();setRows(unwrap(r.data))}catch(e){setError(e.response?.data?.message||e.message||'Unable to load roles')}};useEffect(()=>{load()},[]);const save=async e=>{e.preventDefault();try{if(editing)await roleApi.update(editing.id,form);else await roleApi.create(form);setOpen(false);setEditing(null);await load()}catch(x){setError(x.response?.data?.message||x.message||'Unable to save role')}};return <Box><Box display="flex" alignItems="center" mb={2}><Box flex={1}><Typography variant="h4" fontWeight={700}>Roles</Typography><Typography color="text.secondary">Manage CRM access roles</Typography></Box><Button variant="contained" onClick={()=>{setEditing(null);setForm({name:'',code:'',description:''});setOpen(true)}}>Add Role</Button></Box>{error&&<Alert severity="error" sx={{mb:2}}>{error}</Alert>}<Paper><Table><TableHead><TableRow><TableCell>Name</TableCell><TableCell>Code</TableCell><TableCell>Description</TableCell><TableCell>Actions</TableCell></TableRow></TableHead><TableBody>{rows.map(r=><TableRow key={r.id}><TableCell>{r.name}</TableCell><TableCell>{r.code}</TableCell><TableCell>{r.description}</TableCell><TableCell><Button size="small" onClick={()=>{setEditing(r);setForm({name:r.name||'',code:r.code||'',description:r.description||''});setOpen(true)}}>Edit</Button></TableCell></TableRow>)}</TableBody></Table></Paper><Dialog open={open} onClose={()=>setOpen(false)} fullWidth maxWidth="sm"><DialogTitle>{editing?'Edit Role':'Add Role'}</DialogTitle><form onSubmit={save}><DialogContent><TextField fullWidth required sx={{mb:2}} label="Role Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><TextField fullWidth required sx={{mb:2}} label="Code" value={form.code} onChange={e=>setForm({...form,code:e.target.value.toUpperCase()})}/><TextField fullWidth multiline label="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></DialogContent><DialogActions><Button onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit" variant="contained">Save</Button></DialogActions></form></Dialog></Box>}
